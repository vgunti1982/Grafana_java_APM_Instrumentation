spring:
  application:
    name: agent-app

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

logging:
  file:
    name: logs/agent-app.log
  pattern:
    level: "%5p trace_id=%X{trace_id:-} span_id=%X{span_id:-}"

