spring:
  application:
    name: api-app

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

logging:
  file:
    name: logs/api-app.log
  pattern:
    level: "%5p trace_id=%X{trace_id:-} span_id=%X{span_id:-}"

